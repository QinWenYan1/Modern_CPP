# AGENTS.md

> 本文件面向 AI 编程助手（agents），假定读者对本项目一无所知。阅读后可快速理解仓库定位、内容组织方式与编辑规范。

---

## 📌 项目概览

这是一个 **C++ 学习笔记仓库** —— 按章节组织的 Markdown 学习笔记 + 习题代码实现。**它不是软件工程项目**：没有构建系统（无 `pyproject.toml` / `package.json` / `Cargo.toml` / `Makefile`），没有测试套件，没有部署流程，仓库根目录下也没有需要编译运行的程序。

内容来源分两部分：

- **第 1–16 章（基础 / 标准库 / 类和面向对象）**：《C++ Primer（第 5 版）》的读书笔记，已全部完成。
- **第 17 章及以后（C++ 拓展）**：《C++ Primer》之外的扩展内容：第 17 章为**并发与并行**（素材：《现代 C++ 教程：高速上手 C++》第 7 章，即根目录下的 PDF），第 18 章为**开发工具链**（静态库/动态库、CMake），后续计划加入 *Effective Modern C++*。拓展章节**不对应《C++ Primer》的章节编号**。

仓库主要文档语言为**中文**，关键术语要求中英文双语标注（如「迭代器(iterator)」）。所有笔记、README 及本文件均应使用中文撰写。

## 🗂️ 目录结构与模块划分

```
ChapterNN_topic/                      # 每章一个目录，如 Chapter07_classes/
├── README.md                         # 章节导航页（知识点索引 + 速查 + 上下章链接）
├── note_N_M_descriptive_name.md      # 小节笔记，命名：note_<章>_<节>_<主题>.md
├── note_N_chapter_summary.md         # 章节总结笔记（仅教材章节 1–16 有）
└── exercises/                        # 习题代码
    ├── exercise_N_M.cpp              # 命名：exercise_<章>_<题号>.cpp
    ├── exercise_N_M-K.cpp            # 连续题号用区间，如 exercise_12_6-7.cpp
    └── exercise_N_M/                 # 多文件习题用同名子目录（如 exercise_1_20-24/）
```

- 章节目录命名格式：`ChapterNN_snake_case_topic/`（两位数字补零），例如 `Chapter16_templates_and_generic_programming/`。
- 部分习题是文字作答，使用 `.md` 后缀（如 `exercise_16_1.md`）；个别习题文件命名不完全规范（如 `exercise_1_1_1.cpp`、`exercise_16_6_7.cpp`），**新文件请遵循规范命名，但不要批量重命名旧文件**。
- 根目录另有：`README.md`（总导航 + 内嵌的笔记生成提示词）、`CLAUDE.md`（与本文内容互补的 Claude Code 指引）、`.gitignore`（仅忽略 `.vscode/`）。

## 🔨 构建与运行

- **仓库本身无需构建**。唯一需要动手编译的场景是验证 `exercises/` 下的习题代码，直接用编译器单文件编译即可：

  ```bash
  g++ -std=c++17 -Wall -g exercise_1_3.cpp -o /tmp/ex1_3
  ```

  章节笔记中的工具链示例（g++ / 静态库与动态库 / CMake）见 `Chapter18_extension/README.md` 的「核心速查」部分。
- **没有测试策略**：没有单元测试、CI 或 lint 配置。验收标准是「习题能编译、行为正确」以及「笔记符合模板」。

## 📝 笔记格式规范（最重要）

每个小节笔记（`note_*.md`）必须遵循根目录 `README.md` 中定义的严格模板：

1. **头部**：章节/小节编号、标题、英文原标题；
2. **核心概念总览**：知识点有序列表 + 锚点链接（锚点用 `id1`、`id2`… 顺序递增）；
3. **知识点详解**：每个知识点含 **理论**（核心总结、关键术语加粗、中英文对照）、**示例代码**（忠实保留教材原样）、**注意点**（⚠️ 警告 / 💡 技巧 / 🔄 知识关联 / 📋 术语提醒）；
4. **核心要点总结**：3–5 条提炼要点；
5. **考试速记版**：精简规则、易混淆对比表、常见陷阱、记忆口诀。

排序与合并策略**因内容来源而异**：

- **教材笔记（第 1–16 章）**：严格保持原文段落顺序，禁止逻辑重排与合并；表格、代码、警告必须完整保留。
- **拓展笔记（第 17 章+）**：用户偏好合并知识点（如「概念+特点」合为一点），知识点顺序须与原始资料一致；若认为教学上有更好的排序，**先与用户确认再调整**。

其他约定：

- 章节 `README.md` 底部有 `← 上一章` / `→ 下一章` 链接及 `↑ 返回根目录`（`../README.md`）；新增章节时需同时更新根 `README.md` 的章节导航（归入对应部分：基础 / 标准库 / 类和面向对象 / C++ 拓展）。
- 表情符号有固定含义（📘 章节、🧠 总览、✅ 知识点、⚠️ 警告、💡 技巧等），沿用现有风格。

## 🛠️ 新增章节/小节的工作流

1. 创建 `ChapterNN_topic/` 目录；
2. 用 `course-notes-generator` 技能根据素材生成各小节笔记；
3. 用 `chapter-nav-readme` 技能生成章节 `README.md`；
4. 用 `course-root-readme` 技能（或手动）更新根 `README.md` 导航；
5. 习题代码放入 `exercises/`，按 `exercise_N_M.cpp` 命名。

> 上述三个技能已注册，可直接通过 `Skill` 调用。拓展章节的素材通常由用户以**截图批次**提供（如 `Chapter18_extension/images/` 下的 PNG）：截图可能存在扩展名与实际格式不符的情况，先用 `file` 检查；无法直接读取时可用 OCR 兜底：`tesseract <img> stdout -l chi_sim+eng`（已通过 Homebrew 安装中英文语言包）。流程为：生成**草稿笔记** → 用户审阅修订 → 素材齐全后再做章节 README 与根 README 更新。

## ⚠️ 注意事项

- **编辑现有笔记时保持模板结构**，不要随意重排知识点或删除表格/警告内容。
- 笔记之间、笔记与 README 之间使用相对链接互链；移动/重命名文件时同步更新链接。
- 无敏感信息、无外部服务依赖、无安全问题——内容全部为公开教材的学习笔记。
- 修改了本文件涉及的规范（目录结构、命名、笔记模板、工作流）时，请同步更新 `AGENTS.md` 与 `CLAUDE.md`，保持两者一致。
